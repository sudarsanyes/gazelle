<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Gazelle" 
                    xmlns:controls="clr-namespace:Gazelle.Controls">

    <Style TargetType="{x:Type controls:Stepper}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Stepper}">
                    <Border Width="{TemplateBinding Property=Width}"
                            Height="{TemplateBinding Property=Height}">
                        <StackPanel Orientation="Horizontal">
                            <RepeatButton Content="-"
                                          Command="{x:Static controls:Stepper.Decrement}" />
                            <TextBox x:Name="PART_TextBox" Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Value}"
                                     Width="76" />
                            <RepeatButton Content="+"
                                          Command="{x:Static controls:Stepper.Increment}" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
